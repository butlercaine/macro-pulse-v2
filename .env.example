# Environment Variables Configuration
# Copy this file to .env.local and fill in the values
# Never commit .env.local to version control

# =============================================================================
# FRED API Configuration
# =============================================================================
# Get your free API key from: https://fred.stlouisfed.org/docs/api/api.html
#
# Rate limits:
# - Free tier: 120 requests/day
# - Paid tier: 1000 requests/minute
#
# Without this key, the dashboard will use mock data for demonstration.

FRED_API_KEY=your_fred_api_key_here

# =============================================================================
# Vercel Blob Storage Configuration
# =============================================================================
# Required for storing scraped data in Vercel Blob
# Create a Read-Write token in Vercel Dashboard:
# https://vercel.com/docs/storage/vercel-blob/quickstart#create-a-blob-token
#
# Format: vercel_blob_rw_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

BLOB_READ_WRITE_TOKEN=your_blob_read_write_token_here

# Get your store ID from Vercel Dashboard Storage > Blob
# Format: blob_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

BLOB_STORE_ID=your_blob_store_id_here

# =============================================================================
# Revalidation Secret
# =============================================================================
# Secret token for webhook-based ISR revalidation
# Create a random secret: openssl rand -base64 32
#
# Used in: lib/webhook.ts for POST /api/revalidate

REVALIDATION_SECRET=your_revalidation_secret_here

# =============================================================================
# Optional: GitHub Actions Workflow Secrets
# =============================================================================
# For automated daily scraper runs via GitHub Actions
# Add these secrets in GitHub repository settings:
# Settings > Secrets and variables > Actions
#
# - BLOB_READ_WRITE_TOKEN: Same as above, for scraper upload
# - BLOB_STORE_ID: Same as above, for scraper upload
# - DEPLOY_URL: Your Vercel deployment URL (e.g., https://macro-pulse-v2.vercel.app)
# - REVALIDATION_SECRET: Same as above, for cache busting after scrape
#
# Note: These are set in GitHub repository secrets, not .env.local
